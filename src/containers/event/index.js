import React from 'react';
import Layout from '../../components/Layout'
const Index = () => {
    return (
        <Layout>
            this is tab for event
        </Layout>
    );
}

export default Index;
